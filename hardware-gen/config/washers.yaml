fasteners:

  - name: "M8_PlainWasher"
    standard: "ISO7089"   # DIN125A equivalent
    size: "M8"
    # No length for washers; the workbench derives thickness from the standard.
    pipeline:
      export_3d: "step"
      export_2d_views: ["top"]
